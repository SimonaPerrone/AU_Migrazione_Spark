package it.eng.au.ammissibilitaSettlementGas.schema

object TFCFileSchema extends SchemaEnum {
  val n_id_TSG2_file,
  nome_file,
  piva_rdb,
  annomese,
  data_creazione,
  progressivo,
  executionid
  = Value
}
