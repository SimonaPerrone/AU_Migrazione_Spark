# Migrazione Spark 2 -> Spark 3 - aggiustamento-bilanciamento-gas

## 1. Descrizione Progetto

### 1.1 A cosa serve questo progetto (spiegato semplice)

Al momento non è possibile descrivere il comportamento tecnico del progetto perché il repository contiene solo un `README.md` generico e nessun sorgente applicativo.

### 1.2 Cosa sappiamo oggi dal documento esistente

Nel documento attuale è indicato che il progetto:

- non contiene `pom.xml` / `src/` / script Spark;
- non espone entrypoint o pipeline;
- non permette analisi del funzionamento reale.

(Riferimenti: `Documenti per migrazione/7.Migrazione aggiustamento-bilanciamento-gas.md`, `aggiustamento-bilanciamento-gas/README.md`)

### 1.3 Significato dei componenti (in parole non tecniche)

- **`args`**: non determinabile con i file correnti.
- **`driver`**: non determinabile con i file correnti.
- **`factory`**: non determinabile con i file correnti.
- **`flow`**: non determinabile con i file correnti.
- **`model`**: non determinabile con i file correnti.
- **`dao/controller/utility`**: non determinabile con i file correnti.

(Riferimenti: repository privo di codice applicativo)

### 1.4 Stack tecnico attuale e perché va migrato

Stack attuale rilevato:

- Spark: `[NON DISPONIBILE]`
- Scala/Python: `[NON DISPONIBILE]`
- Java: `[NON DISPONIBILE]`
- Build tool: `[NON DISPONIBILE]`

Target:

- Spark: `3.x`
- Scala: `2.12.x` (se Scala)
- Runtime/cluster: CDP 7.1.9 (YARN Spark3 o CDE `spark35-test`)

(Riferimenti: onboarding CDP 7.1.9; nel repository non sono presenti file tecnici)

---

## 2. Funzionamento step by step

Di seguito il flusso logico completo, con spiegazione “cosa succede” e “perché”.

### Step 1 - Avvio job

Non determinabile.

### Step 2 - Lettura parametri e config

Non determinabile.

### Step 3 - Inizializzazione Spark

Non determinabile.

### Step 4 - Lettura input

Non determinabile.

### Step 5 - Pulizia e normalizzazione

Non determinabile.

### Step 6 - Regole di business (flow)

Non determinabile.

### Step 7 - Costruzione dataset finale

Non determinabile.

### Step 8 - Scrittura output

Non determinabile.

### Step 9 - Chiusura e logging

Non determinabile.

(Riferimenti base del flusso: non presenti nel repository corrente)

---

## 3. Modifiche da apportare per componente (guida pratica per principiante)

## 3.1 `pom.xml` - cosa cambiare, dove, e perché

### Obiettivo

Portare il progetto a Spark 3 + stack compatibile.

(Riferimento: non disponibile)

### Checklist operativa

1. Recuperare dal team il codice reale del progetto.
2. Identificare file build (`pom.xml`/`build.sbt`/`requirements.txt`).
3. Applicare upgrade Spark/Scala/Python in base allo stack effettivo.
4. Aggiornare plugin/librerie/runtime.
5. Verificare packaging e deploy.
6. Preparare script run aggiornati.

### Errori tipici da principiante

- migrare “a template” senza codice reale;
- ipotizzare stack tecnico non verificato;
- definire test senza pipeline effettiva.

---

## 3.2 Codice [Scala/Python/Java] - cosa controllare concretamente

### Obiettivo

Rendere il codice compatibile con Spark 3.

(Riferimento: non disponibile)

### Dove intervenire prima

1. entrypoint reale
2. flusso/controller principali
3. dao/reader/writer reali

### Cosa cambiare (pratico)

- Cast espliciti e gestione tipi.
- Date/Timestamp con format espliciti.
- Null handling in join/filtri.
- Allineamento schema prima di union/join.
- Verifica UDF e serializzazione.

### Config transitoria utile

- `spark.sql.legacy.timeParserPolicy` (solo ponte, poi rimuovere)
- eventuali altre `spark.sql.legacy.*`

### Errori tipici da principiante

- cambiare config prima di capire il codice;
- testare solo avvio e non l’output business.

---

## 3.3 Configurazione runtime (properties + spark-submit)

### Obiettivo

Far girare correttamente il job su cluster Spark 3.

(Riferimento: da definire dopo recupero script reali)

### Cosa verificare

1. Script `spark-submit` (class, jar, conf, risorse).
2. Parametri SQL (`spark.sql.adaptive.enabled`, `spark.sql.shuffle.partitions`, ecc.).
3. Compatibilità Hive Metastore/runtime target.
4. Path e permessi input/output/warehouse.

### Vincoli onboarding CDP 7.1.9 da applicare

- YARN: Kerberos (`kinit -kt` + cleanup ticket)
- CDE: `CDE_CREDENTIALS_FILE` + `CDE_CONFIG_PROFILE`
- CDE: no filesystem locale/NFS per dati
- rimozione conf non supportate in CDE (`spark.shuffle.service.enabled=true`)

### Errori tipici da principiante

- riutilizzare script legacy non validi per CDP/CDE;
- non verificare credenziali e permessi Ranger/Knox/TLS.

---

## 3.4 Ordine consigliato di migrazione (molto pratico)

1. Recupera codice e script reali del progetto.
2. Esegui assessment stack attuale.
3. Aggiorna build/dependency.
4. Migra codice impattato Spark3.
5. Aggiorna runtime e sicurezza.
6. Esegui test e regressione.

---

## 4. Test da prevedere (spiegati per chi parte da zero)

## 4.1 Unit test - test piccoli e veloci

- parsing args/config
- utility e trasformazioni isolate
- null/date/decimal/cast

## 4.2 Integration test - test end-to-end

- pipeline completa su dataset campione
- confronto Spark2 vs Spark3:
  - row count
  - chiavi business
  - KPI/aggregati
- verifica schema output (nomi/tipi/partizioni)

(Riferimento: da definire quando disponibili i sorgenti)

## 4.3 Test non funzionali

- performance (tempo/memoria)
- stabilità run ripetute
- idempotenza/recovery

## 4.4 Criteri di accettazione finali

1. Output equivalente (scostamenti documentati)
2. Nessun errore runtime su Spark 3
3. Stabilità su almeno 3 run consecutivi

(Riferimento: da definire con baseline reale)

---

## 5. Glossario minimo (per orientarti subito)

- **Batch job**: processo che elabora dati a blocchi e termina.
- **SparkSession**: oggetto principale per usare Spark SQL/DataFrame.
- **DAO**: componente che legge/scrive dati da/verso sorgenti.
- **Flow**: sequenza di trasformazioni business.
- **Schema**: struttura colonne + tipi di un dataset.
- **Idempotenza**: rieseguire il job senza effetti collaterali non voluti.
