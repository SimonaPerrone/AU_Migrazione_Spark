include "ammissibilita.conf"

hive{
  table {
    au {
    flusso_misure_interconnessione=${hive.table.au.flusso_misure_interconnessione}
    aggregazioni_misure_quartorarie=${hive.table.au.aggregazioni_misure_quartorarie}
    v_aggregazioni_misure_quartorarie=${hive.table.au.v_aggregazioni_misure_quartorarie}
    v_aggregazione_misure_ip=${hive.table.au.v_aggregazione_misure_ip}
    pod_connessione=${hive.table.au.pod_connessione}
    flusso_misure_smis=${hive.table.au.flusso_misure_smis}
    flusso_misure_noaggr=${hive.table.au.flusso_misure_noaggr}
    flussi_tecnici=${hive.table.au.flussi_tecnici}
    }
    rcu {
    rcu_pod_p=${hive.table.rcu.rcu_pod_p}
    pod_connessione=${hive.table.au.pod_connessione}
    rcu_azienda_p=${hive.table.rcu.rcu_azienda_p}
    rcu_pod_interconnessione_p=${hive.table.rcu.rcu_pod_interconnessione_p}
    rcu_pod_distr_p=${hive.table.rcu.rcu_pod_distr_p}
    }
    rcus {
    rcus_pod_distr=${hive.table.rcus.rcus_pod_distr}
    }
    erp {
    erp_aggregato_o=${hive.table.erp.erp_aggregato_o}
    erp_aggregato_ip_o=${hive.table.erp.erp_aggregato_ip_o}
    erp_validated_int=${hive.table.erp.erp_validated_int}
    erp_dettaglio_int=${hive.table.erp.erp_dettaglio_int}
    erp_aggregato_int=${hive.table.erp.erp_aggregato_int}
    erp_validated_mis_no=${hive.table.erp.erp_validated_mis_no}
    erp_consumption_no=${hive.table.erp.erp_consumption_no}
    erp_daily_dima=${hive.table.erp.erp_daily_dima}
    erp_daily_no=${hive.table.erp.erp_daily_no}
    erp_aggregato_no=${hive.table.erp.erp_aggregato_no}
    }
    tratt_pod {
        tratt_pod_all_annomese_partitioned=${hive.table.tratt_pod.tratt_pod_all_annomese_partitioned}
        }
  }
}
