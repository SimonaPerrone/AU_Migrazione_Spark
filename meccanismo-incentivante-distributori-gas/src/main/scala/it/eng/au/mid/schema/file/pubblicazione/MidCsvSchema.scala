package it.eng.au.mid.schema.file.pubblicazione

import it.eng.au.mid.schema.SchemaEnum

object MidCsvSchema extends SchemaEnum {
  val
  piva_distr,
  piva_udd,
  riga_file,
  progressivo_file
  = Value
}

