package it.eng.au.mid.schema.hive.mid

import it.eng.au.mid.schema.SchemaEnum

object Mid1DettaglioSchema extends SchemaEnum {
  val
  pdr,
  contatore,
  piva_id,
  piva_udd,
  cod_remi,
  gdm,
  alpha,
  executionid_mid_contatori,
  annomese,
  executionid
  = Value
}
