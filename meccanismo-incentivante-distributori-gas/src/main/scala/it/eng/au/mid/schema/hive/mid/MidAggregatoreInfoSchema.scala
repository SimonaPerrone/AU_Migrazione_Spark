package it.eng.au.mid.schema.hive.mid

import it.eng.au.mid.schema.SchemaEnum

object MidAggregatoreInfoSchema extends SchemaEnum {
  val
  operation_name,
  nome_file,
  path,
  tipo_dest,
  piva_dest,
  piva_id_file,
  piva_udd_file,
  data_caricamento,
  executionid_mid_dettaglio,
  executionid
  = Value
}
