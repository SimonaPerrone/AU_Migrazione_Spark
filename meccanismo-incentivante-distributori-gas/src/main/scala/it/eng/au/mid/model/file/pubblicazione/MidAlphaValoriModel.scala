package it.eng.au.mid.model.file.pubblicazione

case class MidAlphaValoriModel(
                                gdm: String,
                                alpha: Int
                              )
