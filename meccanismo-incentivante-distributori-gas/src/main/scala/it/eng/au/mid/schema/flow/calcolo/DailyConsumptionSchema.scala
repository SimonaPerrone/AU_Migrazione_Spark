package it.eng.au.mid.schema.flow.calcolo

import it.eng.au.mid.schema.SchemaEnum

object DailyConsumptionSchema extends SchemaEnum {
  val
  pdr,
  date,
  annomese,
  pivadistr,
  pivaudd,
  codremi,
  classemisuratore,
  treatment,
  session,
  executionid
  = Value
}
