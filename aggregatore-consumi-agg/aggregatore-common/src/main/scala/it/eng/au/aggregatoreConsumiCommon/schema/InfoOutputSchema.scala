package it.eng.au.aggregatoreConsumiCommon.schema

object InfoOutputSchema extends SchemaEnum {
  val
  daily_consumption_execution_id,
  operation_name,
  base_name,
  path_name,
  load_date,
  partition_date
  = Value
}
